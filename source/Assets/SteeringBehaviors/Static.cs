using UnityEngine;

namespace Flocking
{
    public struct Static
    {
        public Vector3 position; // the position in world coordinates
        public Vector3 orientation; // the vector that shows where we are looking
    }
}